<form method="post" action="{{ path('app_billets_avion_delete', {'id': billets_avion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ billets_avion.id) }}">
    <button class="btn">Delete</button>
</form>
