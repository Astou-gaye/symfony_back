{% extends 'base.html.twig' %}

{% block title %}Modifier session{% endblock %}
{% block page_title %}Modifier session{% endblock %}
{% block body %}
<div class="card">
  <div class="card-header d-flex justify-content-between align-items-center">
    <span class="fw-bold">Édition session</span>
    <a href="{{ path('app_session_index') }}" class="btn btn-sm btn-outline-primary"><i class="bx bx-arrow-back"></i> Retour</a>
  </div>
  <div class="card-body">
    {{ include('session/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    <div class="mt-3">
      {{ include('session/_delete_form.html.twig') }}
    </div>
  </div>
</div>
{% endblock %}
