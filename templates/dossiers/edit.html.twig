{% extends 'base.html.twig' %}

{% block title %}Modifier dossier{% endblock %}
{% block page_title %}Modifier dossier{% endblock %}
{% block body %}
<div class="card">
  <div class="card-header d-flex justify-content-between align-items-center">
    <span class="fw-bold">Édition dossier</span>
    <a href="{{ path('app_dossiers_index') }}" class="btn btn-sm btn-outline-primary"><i class="bx bx-arrow-back"></i> Retour</a>
  </div>
  <div class="card-body">
    {{ include('dossiers/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    <div class="mt-3">
      {{ include('dossiers/_delete_form.html.twig') }}
    </div>
  </div>
</div>
{% endblock %}
