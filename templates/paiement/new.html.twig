{% extends 'base.html.twig' %}

{% block title %}Nouveau paiement{% endblock %}
{% block page_title %}Créer un paiement{% endblock %}
{% block body %}
<div class="card">
  <div class="card-header d-flex justify-content-between align-items-center">
    <span class="fw-bold">Création paiement</span>
    <a href="{{ path('app_paiement_index') }}" class="btn btn-sm btn-outline-primary"><i class="bx bx-arrow-back"></i> Retour</a>
  </div>
  <div class="card-body">
    {{ include('paiement/_form.html.twig') }}
  </div>
</div>
{% endblock %}
