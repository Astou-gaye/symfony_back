{% extends 'base.html.twig' %}

{% block title %}Nouveau voyage{% endblock %}
{% block page_title %}Créer un voyage{% endblock %}
{% block body %}
<div class="card">
  <div class="card-header d-flex justify-content-between align-items-center">
    <span class="fw-bold">Création voyage</span>
    <a href="{{ path('app_voyage_index') }}" class="btn btn-sm btn-outline-primary"><i class="bx bx-arrow-back"></i> Retour</a>
  </div>
  <div class="card-body">
    {{ include('voyage/_form.html.twig') }}
  </div>
</div>
{% endblock %}
