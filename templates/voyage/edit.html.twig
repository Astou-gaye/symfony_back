{% extends 'base.html.twig' %}

{% block title %}Modifier voyage{% endblock %}
{% block page_title %}Modifier voyage{% endblock %}
{% block body %}
<div class="card">
  <div class="card-header d-flex justify-content-between align-items-center">
    <span class="fw-bold">Édition voyage</span>
    <a href="{{ path('app_voyage_index') }}" class="btn btn-sm btn-outline-primary"><i class="bx bx-arrow-back"></i> Retour</a>
  </div>
  <div class="card-body">
    {{ include('voyage/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    <div class="mt-3">
      {{ include('voyage/_delete_form.html.twig') }}
    </div>
  </div>
</div>
{% endblock %}
